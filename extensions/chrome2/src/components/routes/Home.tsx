import React from 'react'
import { Button } from '../ui/button'

export const HomeRoute: React.FC = () => {

  return (
    <div className="flex flex-col gap-3">
      <Button variant={"success"}>Tailor resume for this page</Button>
    </div>
  )
}
